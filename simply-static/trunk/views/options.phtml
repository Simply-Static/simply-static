<?php
/**
 * @package Simply_Static
 */
?>

<div class="wrap">

	<h2><?php echo __( 'Simply Static', $this->slug ); ?></h2>

		<form id="options" method="post" action="">

			<table class="form-table">
				<tbody>
					<tr>
						<th>
							<label for="origin_host"><?php echo __( 'Origin URL', $this->slug );?></label>
						</th>
						<td>
							<select id="originScheme" name="origin_scheme" disabled>
								<option value="<?php echo $this->origin_scheme; ?>"><?php echo $this->origin_scheme; ?>://</option>
							</select>
							<input aria-describedby="originHostHelpBlock" type="text" id="originHost" name="origin_host" value="<?php echo esc_attr( $this->origin_host ) ?>" size="50" disabled />
							<p id="originHostHelpBlock" class="help-block">This is some helpful text. <a href="#">Click here to change these settings.</a></p>
						</td>
					</tr>
					<tr>
						<th>
							<label for="destination_host"><?php echo __( 'Destination URL', $this->slug );?></label>
						</th>
						<td>
							<select id="destinationScheme" name="destination_scheme">
								<option value="http" <?php if ( $this->destination_scheme == 'http' ) { echo 'selected="selected"'; } ?>>http://</option>
								<option value="https" <?php if ( $this->destination_scheme == 'https' ) { echo 'selected="selected"'; } ?>>https://</option>
							</select>
							<input aria-describedby="destinationHostHelpBlock" type="text" id="destinationHost" name="destination_host" value="<?php echo esc_attr( $this->destination_host ) ?>" size="50" />
							<p id="destinationHostHelpBlock" class="help-block">This is some helpful text.</p>
						</td>
					</tr>
					<tr>
						<th>
							<label for="additional_urls"><?php echo __( 'Additional URLs', $this->slug );?></label>
						</th>
						<td>
							<textarea class="widefat" name="additional_urls" id="additionalUrls" rows="5" cols="10"><?php echo esc_html( $this->additional_urls ) ?></textarea>
							<p id="originHostHelpBlock" class="help-block">e.g. /page-im-not-linking-to or <?php echo $this->origin_scheme; ?>://<?php echo $this->origin_host; ?>/other-hidden-page</p>
						</td>
					</tr>
					<tr>
						<th>

						</th>
						<td>
							<input name="generate_zip" id="generateZip" value="1" type="checkbox" <?php if ( $this->generate_zip == 1 ) { echo "checked"; } ?> />
							<label for="generate_zip"><?php echo __( 'Generate ZIP archive', $this->slug );?></label>
						</td>
					</tr>
					<tr>
						<th>

						</th>
						<td>
							<input name="retain_static_files" id="retainStaticFiles" value="1" type="checkbox" <?php if ( $this->retain_static_files == 1 ) { echo "checked"; } ?> />
							<label for="retain_static_files"><?php echo __( 'Retain generated static files on server', $this->slug );?></label>
						</td>
					</tr>
				</tbody>
			</table>

			<p class="submit">
				<?php wp_nonce_field( $this->slug ) ?>
				<input type="hidden" name="action" value="generate" />
				<input class="button-primary" type="submit" name="save" value="<?php echo __( 'Save', $this->slug );?>" />
			</p>

		</form>

</div>
