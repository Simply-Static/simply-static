---
wordpress_sites:
  - { 'name' : 'wordpress.local', 'port' : '80', database_name: 'wordpress' }

env: development

database_host: 127.0.0.1

mysql_root_username: root
mysql_root_password: password

wp_db_username: "{{ mysql_root_username }}"
wp_db_password: "{{ mysql_root_password }}"
